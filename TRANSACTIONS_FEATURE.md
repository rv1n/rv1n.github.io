# История покупок/продаж - Документация

## Описание

Полнофункциональный журнал для ведения истории всех операций покупки и продажи акций. Позволяет отслеживать все транзакции, редактировать их и фильтровать по различным критериям.

## Основные возможности

### 1. Учёт транзакций
- ➕ **Добавление операций** - покупка или продажа акций
- ✏️ **Редактирование** - изменение любой информации о транзакции
- 🗑️ **Удаление** - удаление ошибочных или ненужных записей

### 2. Полная информация о каждой операции
- **Дата и время** - точное время совершения операции
- **Тикер** - символ акции (например, SBER, GAZP)
- **Название компании** - полное название эмитента
- **Вид операции** - покупка (зелёный) или продажа (красный)
- **Цена** - цена за одну акцию в рублях
- **Количество** - количество акций
- **Сумма** - автоматически рассчитывается (цена × количество)
- **Заметки** - дополнительные комментарии

### 3. Фильтрация
- 🔍 **По тикеру** - просмотр операций по конкретной акции
- 📊 **По типу операции** - только покупки или только продажи
- 📅 **По датам** - выбор периода (дата от/до)
- 🔄 **Сброс фильтров** - быстрый возврат к полному списку

### 4. Удобный интерфейс
- Таблица с автоматической сортировкой (новые операции сверху)
- Цветовая индикация типов операций
- Модальные окна для добавления/редактирования
- Автоматический расчёт суммы при вводе цены и количества

## Структура БД

### Таблица `transactions`

```sql
CREATE TABLE transactions (
    id INTEGER PRIMARY KEY,
    date DATETIME NOT NULL,
    ticker VARCHAR(20) NOT NULL,
    company_name VARCHAR(200),
    operation_type ENUM('BUY', 'SELL') NOT NULL,
    price FLOAT NOT NULL,
    quantity FLOAT NOT NULL,
    total FLOAT NOT NULL,
    notes VARCHAR(500)
);

-- Индексы для быстрого поиска
CREATE INDEX idx_ticker ON transactions(ticker);
CREATE INDEX idx_date ON transactions(date);
CREATE INDEX idx_operation_type ON transactions(operation_type);
```

## API Endpoints

### 1. GET `/api/transactions`
Получить список всех транзакций с фильтрацией

**Query параметры:**
- `ticker` (опционально) - фильтр по тикеру
- `operation_type` (опционально) - "Покупка" или "Продажа"
- `date_from` (опционально) - дата от (формат: YYYY-MM-DD)
- `date_to` (опционально) - дата до (формат: YYYY-MM-DD)

**Пример запроса:**
```
GET /api/transactions?ticker=SBER&operation_type=Покупка&date_from=2026-01-01
```

**Ответ:**
```json
{
    "success": true,
    "transactions": [
        {
            "id": 1,
            "date": "2026-01-15 14:30:00",
            "ticker": "SBER",
            "company_name": "Сбербанк",
            "operation_type": "Покупка",
            "price": 285.50,
            "quantity": 10,
            "total": 2855.00,
            "notes": "Покупка на просадке"
        }
    ]
}
```

### 2. POST `/api/transactions`
Добавить новую транзакцию

**Тело запроса:**
```json
{
    "date": "2026-01-15 14:30:00",
    "ticker": "SBER",
    "company_name": "Сбербанк",
    "operation_type": "Покупка",
    "price": 285.50,
    "quantity": 10,
    "notes": "Покупка на просадке"
}
```

**Ответ:**
```json
{
    "success": true,
    "message": "Транзакция успешно добавлена",
    "transaction": { ... }
}
```

### 3. PUT `/api/transactions/<id>`
Обновить существующую транзакцию

**Тело запроса:**
```json
{
    "price": 290.00,
    "quantity": 15,
    "notes": "Скорректированные данные"
}
```

**Ответ:**
```json
{
    "success": true,
    "message": "Транзакция успешно обновлена",
    "transaction": { ... }
}
```

### 4. DELETE `/api/transactions/<id>`
Удалить транзакцию

**Ответ:**
```json
{
    "success": true,
    "message": "Транзакция успешно удалена"
}
```

## Использование

### Добавление транзакции

1. Перейдите на вкладку **"💼 История покупок/продаж"**
2. Нажмите кнопку **"➕ Добавить операцию"**
3. Заполните форму:
   - Дата и время операции (по умолчанию - текущее время)
   - Тикер акции
   - Название компании (опционально)
   - Вид операции (Покупка/Продажа)
   - Цена за акцию
   - Количество акций
   - Заметки (опционально)
4. Сумма рассчитается автоматически
5. Нажмите **"Добавить"**

### Редактирование транзакции

1. В таблице транзакций найдите нужную запись
2. Нажмите кнопку **✏️** (редактировать)
3. Измените нужные поля
4. Нажмите **"Сохранить"**

### Удаление транзакции

1. В таблице транзакций найдите нужную запись
2. Нажмите кнопку **🗑️** (удалить)
3. Подтвердите удаление

### Использование фильтров

1. В верхней части страницы выберите фильтры:
   - **Тикер** - для просмотра операций по конкретной акции
   - **Вид операции** - только покупки или продажи
   - **Дата от/до** - для выбора временного периода
2. Фильтры применяются автоматически
3. Для сброса всех фильтров нажмите **"🔄 Сбросить фильтры"**

## Примеры использования

### Сценарий 1: Отслеживание покупок Сбербанка
1. В фильтре "Тикер" выберите **SBER**
2. В фильтре "Вид операции" выберите **Покупка**
3. Таблица покажет все покупки акций Сбербанка

### Сценарий 2: Просмотр операций за месяц
1. В поле "Дата от" введите **2026-01-01**
2. В поле "Дата до" введите **2026-01-31**
3. Таблица покажет все операции за январь 2026

### Сценарий 3: Анализ всех продаж
1. В фильтре "Вид операции" выберите **Продажа**
2. Таблица покажет все операции продажи
3. Можно быстро оценить общий объём реализации активов

## Преимущества

### Для инвестора
- 📊 Полная история всех сделок в одном месте
- 💰 Автоматический расчёт сумм транзакций
- 🔍 Быстрый поиск нужных операций
- 📝 Возможность добавлять комментарии к сделкам

### Для анализа
- Можно отследить среднюю цену покупки акции за период
- Удобно анализировать стратегию покупок/продаж
- Легко найти конкретную операцию по датам
- Визуальное различие покупок (зелёный) и продаж (красный)

### Для учёта
- Точный учёт всех операций с ценными бумагами
- Возможность исправления ошибок
- Хранение дополнительной информации в заметках
- Экспорт данных для налоговой отчётности (можно добавить)

## Архитектура

### Файлы
- `models/transaction.py` - модель БД для транзакций
- `app.py` - API endpoints для CRUD операций
- `templates/index.html` - UI с формами и таблицей
- `static/css/style.css` - стили для транзакций
- `static/js/main.js` - JavaScript функции

### Типы операций
```python
class TransactionType(enum.Enum):
    BUY = "Покупка"
    SELL = "Продажа"
```

## Возможные расширения

### 1. Статистика по транзакциям
- Общий объём покупок/продаж
- Средняя цена покупки по каждому тикеру
- Реализованная прибыль/убыток
- График изменения объёма портфеля

### 2. Импорт/Экспорт
- Импорт сделок из Excel/CSV
- Экспорт в различные форматы
- Интеграция с брокерскими отчётами

### 3. Аналитика
- Расчёт реализованной прибыли (FIFO/LIFO)
- Средняя цена покупки с учётом комиссий
- Сравнение цен покупки с текущими котировками
- Выявление удачных/неудачных сделок

### 4. Уведомления
- Напоминания о планируемых покупках
- Уведомления при достижении целевой цены
- Алерты на значительные изменения портфеля

## Интеграция с портфелем

История транзакций тесно связана с основным портфелем:
- При добавлении покупки можно автоматически обновлять портфель
- При продаже - уменьшать количество акций
- Расчёт средней цены покупки на основе транзакций
- Автоматическое заполнение данных при добавлении операций

## Безопасность данных

- Все транзакции хранятся в локальной БД SQLite
- Данные не передаются на внешние серверы
- Возможность создания резервных копий БД
- Восстановление данных из бэкапа

## Заключение

Модуль "История покупок/продаж" - это мощный инструмент для ведения полного учёта операций с ценными бумагами. Он позволяет не только фиксировать сделки, но и анализировать свою инвестиционную стратегию, находить закономерности и принимать более обоснованные решения.
