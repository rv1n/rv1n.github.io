# 🔧 Настройка Favicon на сервере

## Проблема: Favicon не отображается на сервере

## Решение:

### Шаг 1: Убедитесь, что файл favicon.svg загружен на сервер

Проверьте наличие файла на сервере:

```bash
# Подключитесь к серверу по SSH
ssh ваш-пользователь@5.129.221.111

# Перейдите в директорию проекта
cd /path/to/att1

# Проверьте наличие файла
ls -la static/favicon.svg
```

Если файла нет, загрузите его:

```bash
# С локального ПК (из директории проекта)
scp static/favicon.svg ваш-пользователь@5.129.221.111:/path/to/att1/static/
```

### Шаг 2: Проверьте права доступа

```bash
# На сервере
chmod 644 static/favicon.svg
chown ваш-пользователь:ваш-пользователь static/favicon.svg
```

### Шаг 3: Проверьте, что Nginx не блокирует статические файлы

Проверьте конфиг Nginx (`/etc/nginx/sites-available/att1`):

```nginx
server {
    # ... другие настройки ...
    
    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
    
    # Явная обработка favicon (опционально, но рекомендуется)
    location = /favicon.ico {
        proxy_pass http://127.0.0.1:8000/favicon.ico;
        access_log off;
        log_not_found off;
    }
    
    location = /favicon.svg {
        proxy_pass http://127.0.0.1:8000/favicon.svg;
        access_log off;
        log_not_found off;
    }
}
```

После изменений:

```bash
sudo nginx -t
sudo systemctl reload nginx
```

### Шаг 4: Перезапустите приложение

```bash
# Если используете systemd
sudo systemctl restart gunicorn
# или
sudo systemctl restart ваш-сервис

# Или если запускаете вручную
# Остановите и запустите заново
```

### Шаг 5: Проверьте доступность файла

Проверьте в браузере или через curl:

```bash
# Проверка SVG favicon
curl -I http://5.129.221.111/favicon.svg

# Должен вернуть:
# HTTP/1.1 200 OK
# Content-Type: image/svg+xml
```

Или откройте в браузере:
- `http://5.129.221.111/favicon.svg` - должен показать иконку
- `http://5.129.221.111/favicon.ico` - должен скачать файл

### Шаг 6: Очистите кэш браузера

После всех изменений:
1. Откройте сайт
2. Нажмите `Ctrl+Shift+R` (Windows) или `Cmd+Shift+R` (Mac) для жесткой перезагрузки
3. Или очистите кэш браузера полностью

### Шаг 7: Проверьте консоль браузера

Откройте DevTools (F12) → вкладка Console и Network:
- Проверьте, нет ли ошибок 404 для favicon
- Проверьте, что запрос к `/favicon.svg` возвращает 200 OK

## Альтернативное решение: Использовать абсолютный путь

Если проблемы продолжаются, можно использовать абсолютный путь в HTML:

```html
<link rel="icon" type="image/svg+xml" href="https://5.129.221.111/favicon.svg">
```

Но лучше исправить проблему с путями.

## Быстрая проверка всех шагов:

```bash
# 1. Файл существует?
ls -la static/favicon.svg

# 2. Права доступа правильные?
stat static/favicon.svg

# 3. Flask может отдать файл?
curl http://127.0.0.1:8000/favicon.svg

# 4. Nginx проксирует правильно?
curl http://5.129.221.111/favicon.svg

# 5. Приложение перезапущено?
sudo systemctl status gunicorn
```

## Если ничего не помогает:

Создайте простой favicon.ico файл и поместите его в корень проекта, затем добавьте в Nginx:

```nginx
location = /favicon.ico {
    root /path/to/att1/static;
    access_log off;
    log_not_found off;
}
```
