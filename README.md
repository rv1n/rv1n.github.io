# 📈 Портфель акций Московской биржи

Веб-приложение для отслеживания котировок акций Московской биржи в реальном времени с использованием официального ISS API MOEX.

## 🚀 Функциональность

### Основные возможности

- ✅ **Получение данных с MOEX** - Использование официального ISS API для получения актуальных котировок
- ✅ **База данных портфеля** - Хранение ваших активов в SQLite
- ✅ **Расчеты P/L** - Автоматический расчет прибыли/убытка по каждой позиции и портфелю в целом
- ✅ **Веб-интерфейс** - Современный и удобный интерфейс с автообновлением
- ✅ **Live-обновление** - Автоматическое обновление котировок каждые 5 минут без перезагрузки страницы
- ✅ **Ручное обновление** - Кнопка для мгновенного обновления данных
- ✅ **Обновление при изменении цен** - Автоматическое обновление интерфейса при обнаружении изменений цен (проверка каждые 5 секунд)
- ✅ **Расчет средней цены** - Автоматический расчет средней цены покупки при добавлении одинаковых тикеров
- ✅ **Цветовая индикация** - Зеленый для прибыли, красный для убытка

### Управление портфелем

- Добавление новых позиций (тикер, количество, цена покупки)
- Добавление одинаковых тикеров с автоматическим расчетом средней цены покупки
- Редактирование существующих позиций
- Удаление позиций из портфеля
- Просмотр актуальных цен и изменений

## 📋 Требования

- Python 3.11+
- pip (менеджер пакетов Python)

## 🔧 Установка и запуск

### 1. Клонирование/скачивание проекта

```bash
# Если проект уже скачан, перейдите в его директорию
cd att1
```

### 2. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 3. Запуск приложения

```bash
python app.py
```

Приложение запустится на `http://localhost:5000`

### 4. Открытие в браузере

Откройте браузер и перейдите по адресу:
```
http://localhost:5000
```

## 📁 Структура проекта

```
att1/
├── app.py                 # Главный файл Flask приложения
├── requirements.txt        # Зависимости проекта
├── portfolio.db           # База данных SQLite (создается автоматически)
├── models/                # Модели базы данных
│   ├── __init__.py
│   ├── database.py        # Настройка подключения к БД
│   └── portfolio.py       # Модель портфеля
├── services/              # Сервисы для работы с внешними API
│   ├── __init__.py
│   └── moex_service.py    # Сервис для работы с MOEX ISS API
├── templates/             # HTML шаблоны
│   └── index.html         # Главная страница
└── static/                # Статические файлы
    ├── css/
    │   └── style.css      # Стили приложения
    └── js/
        └── main.js        # JavaScript для автообновления
```

## 🏗️ Архитектура

### Разделение ответственности

- **models/** - Модели базы данных (SQLAlchemy)
- **services/** - Бизнес-логика работы с внешними API
- **routes** (в app.py) - API endpoints и маршруты
- **templates/** - HTML шаблоны
- **static/** - CSS и JavaScript

### Ключевые компоненты

#### 1. MOEXService (`services/moex_service.py`)

Сервис для работы с MOEX ISS API:
- Получение текущих цен акций
- Кэширование запросов (2 секунды) для оптимизации
- Обработка ошибок API
- Поддержка расширения для других бирж

**Пример использования:**
```python
from services.moex_service import MOEXService

moex = MOEXService()
data = moex.get_current_price('SBER')
# Возвращает: {'price': 250.50, 'change': 2.30, 'change_percent': 0.93, ...}
```

#### 2. Portfolio Model (`models/portfolio.py`)

Модель для хранения позиций в портфеле:
- `id` - Уникальный идентификатор
- `ticker` - Тикер акции (SBER, GAZP и т.д.)
- `company_name` - Название компании
- `quantity` - Количество акций
- `average_buy_price` - Средняя цена покупки
- `date_added` - Дата добавления

#### 3. API Endpoints

- `GET /api/portfolio` - Получить весь портфель с актуальными ценами
- `POST /api/portfolio` - Добавить новую позицию
- `PUT /api/portfolio/<id>` - Обновить позицию
- `DELETE /api/portfolio/<id>` - Удалить позицию
- `GET /api/quote/<ticker>` - Получить котировку для тикера

## 📊 Пример запроса к MOEX API

Приложение использует следующий endpoint MOEX ISS API:

```
GET https://iss.moex.com/iss/engines/stock/markets/shares/securities/{TICKER}.json
```

**Параметры:**
- `iss.meta=off` - Отключение метаданных
- `iss.json=extended` - Расширенный формат JSON
- `marketdata.columns=LAST,OPEN,CHANGE,LASTTOPREVPRICE,VALTODAY` - Необходимые колонки

**Пример ответа:**
```json
[
  {},
  {
    "securities": [...],
    "marketdata": [
      {
        "LAST": 250.50,
        "OPEN": 248.20,
        "CHANGE": 2.30,
        "LASTTOPREVPRICE": 2.30,
        "VALTODAY": 1500000000
      }
    ]
  }
]
```

## 🎨 Интерфейс

### Основные элементы

1. **Сводка портфеля** - Общая стоимость и прибыль/убыток
2. **Форма добавления** - Быстрое добавление новых позиций
3. **Таблица портфеля** - Детальная информация по каждой позиции:
   - Тикер и название компании
   - Количество акций
   - Текущая цена и изменение
   - Цена покупки
   - Прибыль/убыток (₽ и %)
   - Объем торгов
   - Кнопки редактирования и удаления

### Автообновление

- Данные обновляются каждые 5 минут автоматически (полное обновление)
- Проверка изменений цен каждые 5 секунд (тихое обновление при изменениях)
- Доступна кнопка "Обновить" для ручного обновления данных в любое время
- Таймер обратного отсчета до следующего обновления
- Страница не перезагружается (AJAX)
- Отображается время последнего обновления

## 🔒 Обработка ошибок

Приложение включает обработку следующих ошибок:

- Ошибки подключения к MOEX API
- Неверные тикеры
- Ошибки базы данных
- Валидация входных данных
- Таймауты запросов

## 🚀 Расширение функциональности

### Добавление новых бирж

Архитектура позволяет легко добавить поддержку других бирж:

1. Создайте новый сервис в `services/` (например, `spb_service.py`)
2. Реализуйте интерфейс, аналогичный `MOEXService`
3. Добавьте выбор биржи в интерфейс
4. Используйте соответствующий сервис в зависимости от выбора

### Улучшения

Возможные улучшения:
- WebSocket для реального времени (вместо polling)
- Экспорт портфеля в Excel/CSV
- Графики изменения цен
- История транзакций
- Уведомления о достижении ценовых уровней
- Поддержка других типов активов (облигации, ETF)

## 📝 Лицензия

Проект создан в образовательных целях.

## 🤝 Поддержка

При возникновении проблем:
1. Проверьте, что все зависимости установлены
2. Убедитесь, что порт 5000 свободен
3. Проверьте подключение к интернету (для запросов к MOEX API)
4. Проверьте логи в консоли

## 📚 Дополнительная информация

- [Документация MOEX ISS API](https://www.moex.com/a2193)
- [Flask Documentation](https://flask.palletsprojects.com/)
- [SQLAlchemy Documentation](https://docs.sqlalchemy.org/)

---

**Приятного использования! 📈**
