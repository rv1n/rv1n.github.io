# История изменений

## 31.01.2026 - Изменение расчета изменения цен

### 🎯 Основное изменение

#### Новая логика расчета изменения цен
**До:**
- Изменение цены (абсолютное и процентное) бралось из API MOEX
- API возвращает изменение относительно цены открытия текущего торгового дня
- Не соответствовало нашей логике исторического отслеживания

**После:**
- Изменение цены рассчитывается относительно **последней записи в истории цен** (`price_history`)
- Формула: `изменение = текущая_цена - последняя_залогированная_цена`
- Процент: `изменение_% = (изменение / последняя_залогированная_цена) × 100`
- Более осмысленно для долгосрочного отслеживания портфеля

### 📊 Примеры

#### Сценарий 1: Ежедневное логирование
```
День 1 (00:00): Залогирована цена SBER = 280 ₽
День 1 (15:00): Текущая цена = 285 ₽
→ Изменение: +5 ₽ (+1.79%)

День 2 (00:00): Залогирована цена SBER = 285 ₽
День 2 (15:00): Текущая цена = 283 ₽
→ Изменение: -2 ₽ (-0.70%)
```

#### Сценарий 2: Первое добавление тикера
```
Добавили GAZP в портфель, в истории цен записей нет
Текущая цена = 180 ₽
→ Изменение: 0 ₽ (0%)
(нет базы для сравнения)

После первого логирования (00:00 следующего дня):
Залогирована цена GAZP = 180 ₽
Текущая цена = 182 ₽
→ Изменение: +2 ₽ (+1.11%)
```

### 🔧 Технические детали

#### Изменённые файлы:
1. **`app.py`** - функция `get_portfolio()`
   - Добавлен запрос последней записи из `price_history` для каждого тикера
   - Расчет `price_change` и `price_change_percent` теперь основан на исторических данных
   - Если истории нет → изменение = 0

2. **`services/price_logger.py`** - функция `log_all_prices()`
   - При логировании цены также рассчитывается изменение относительно предыдущей записи
   - Сохраняется в `change` и `change_percent` таблицы `price_history`
   - Улучшенное логирование с выводом изменений

#### Новая логика в коде:

**В `app.py` (получение портфеля):**
```python
# Получаем последнюю цену из истории для расчета изменения
last_history_entry = db_session.query(PriceHistory).filter(
    PriceHistory.ticker == item.ticker
).order_by(PriceHistory.logged_at.desc()).first()

if last_history_entry and current_price > 0:
    last_logged_price = last_history_entry.price
    price_change = current_price - last_logged_price
    price_change_percent = (price_change / last_logged_price * 100) if last_logged_price > 0 else 0
else:
    price_change = 0
    price_change_percent = 0
```

**В `services/price_logger.py` (логирование):**
```python
# Получаем последнюю запись из истории для расчета изменения
last_history_entry = db_session.query(PriceHistory).filter(
    PriceHistory.ticker == ticker
).order_by(PriceHistory.logged_at.desc()).first()

if last_history_entry and current_price > 0:
    last_logged_price = last_history_entry.price
    price_change = current_price - last_logged_price
    price_change_percent = (price_change / last_logged_price * 100) if last_logged_price > 0 else 0
else:
    price_change = 0
    price_change_percent = 0
```

### 💡 Преимущества новой логики

#### Для долгосрочных инвесторов:
- ✅ Видите изменение с момента последней проверки
- ✅ Не зависит от внутридневных колебаний торгового дня
- ✅ Более релевантно для стратегий "купил и держи"

#### Для аналитики:
- ✅ История цен теперь содержит изменения между логированиями
- ✅ Можно отследить динамику изменений за любой период
- ✅ Легко построить графики изменений

#### Для портфеля:
- ✅ Согласованность данных между таблицами
- ✅ Изменения в портфеле = изменения в истории
- ✅ Одна источника правды для всех расчетов

### 📈 Как это работает

#### Последовательность событий:

1. **Ежедневно в 00:00 МСК:**
   - APScheduler запускает `log_all_prices()`
   - Для каждого тикера из портфеля:
     - Получает текущую цену с MOEX
     - Находит последнюю запись в `price_history`
     - Рассчитывает изменение относительно нее
     - Сохраняет новую запись с рассчитанным изменением

2. **При отображении портфеля:**
   - Для каждой позиции:
     - Получает текущую цену с MOEX (с кешированием на 2 сек)
     - Находит последнюю запись в `price_history`
     - Рассчитывает изменение относительно нее
     - Отображает в таблице

3. **Во вкладке "История цен":**
   - Показывает все залогированные цены
   - Каждая запись уже содержит рассчитанное изменение
   - Изменение показывает динамику между логированиями

### ⚠️ Важные замечания

1. **Первое добавление тикера**
   - Пока нет записи в `price_history`, изменение будет 0%
   - После первого логирования (в 00:00 следующего дня) появятся изменения

2. **Ручное логирование**
   - Можно вызвать `POST /api/log-prices-now` для тестирования
   - Это создаст запись в истории прямо сейчас
   - Следующий расчет будет относительно этой записи

3. **Кеширование MOEX API**
   - Запросы к API кешируются на 2 секунды
   - Это не влияет на расчет изменений
   - Изменения рассчитываются для каждого запроса

### 🎯 Рекомендации

#### Для начала работы:
1. Добавьте тикеры в портфель
2. Вызовите ручное логирование: `POST /api/log-prices-now`
3. Теперь будут отображаться изменения относительно залогированных цен

#### Для регулярного использования:
- Просто оставьте приложение работать
- Каждый день в 00:00 МСК цены будут логироваться автоматически
- Изменения будут рассчитываться относительно предыдущего дня

#### Для анализа:
- Используйте вкладку "📊 История цен" для просмотра динамики
- Фильтруйте по тикерам и датам
- Изменения показывают прирост/падение между днями

---

## 29.01.2026 - Интеграция транзакций с портфелем

### 🎯 Основные изменения

#### 1. Изменена логика добавления позиций
**До:**
- Кнопка "Добавить" просто добавляла позицию в портфель
- Не было связи с историей транзакций

**После:**
- Кнопка "Добавить" теперь:
  1. Создаёт транзакцию типа "Покупка" в истории
  2. Добавляет/обновляет позицию в портфеле
- Полная интеграция учёта покупок

#### 2. Убраны кнопки редактирования и удаления из портфеля
**До:**
- В таблице портфеля были кнопки ✏️ (редактировать) и 🗑️ (удалить)
- Можно было вручную изменять или удалять позиции

**После:**
- Кнопки редактирования и удаления убраны
- Портфель изменяется только через покупку/продажу
- Более правильная бухгалтерия: каждое изменение = транзакция

#### 3. Добавлена кнопка продажи 🛒
**Новая функциональность:**
- В таблице портфеля теперь одна кнопка: 🛒 (Продать)
- Иконка корзины из магазина, как запрошено
- Открывает модальное окно продажи

#### 4. Модальное окно продажи
**Функции:**
- Показывает информацию о позиции (тикер, компания, доступное количество)
- Позволяет указать:
  - Количество для продажи (с валидацией)
  - Цену продажи (по умолчанию - текущая рыночная)
  - Заметки к операции
- Автоматически рассчитывает сумму продажи
- Валидация: нельзя продать больше, чем есть

#### 5. Логика продажи
**Что происходит при продаже:**
1. Создаётся транзакция типа "Продажа" в истории
2. Уменьшается количество акций в портфеле
3. Если продали все акции → позиция автоматически удаляется из портфеля
4. Если продали часть → обновляется количество в портфеле

#### 6. Убрана кнопка "Добавить операцию" из истории транзакций
**До:**
- В разделе "История покупок/продаж" была кнопка "➕ Добавить операцию"
- Можно было вручную добавлять транзакции

**После:**
- Кнопка убрана
- Все транзакции создаются автоматически через покупку/продажу
- Раздел истории теперь только для просмотра, редактирования и удаления существующих транзакций

### 📋 Новый рабочий процесс

#### Покупка акций:
1. На главном экране заполните форму:
   - Тикер
   - Название компании (автозаполнение)
   - Категория
   - Количество
   - Цена покупки
2. Нажмите "Добавить"
3. Система автоматически:
   - ✅ Создаст транзакцию покупки
   - ✅ Добавит/обновит позицию в портфеле
   - ✅ Пересчитает среднюю цену (если тикер уже был)

#### Продажа акций:
1. В таблице "Мой портфель" найдите нужную позицию
2. Нажмите кнопку 🛒 (Продать)
3. В открывшемся окне:
   - Проверьте доступное количество
   - Укажите количество для продажи
   - При необходимости измените цену
   - Добавьте заметку (опционально)
4. Нажмите "Продать"
5. Система автоматически:
   - ✅ Создаст транзакцию продажи
   - ✅ Уменьшит количество в портфеле
   - ✅ Удалит позицию, если продали всё

### 💡 Преимущества новой системы

#### Для учёта:
- ✅ Каждое изменение портфеля документируется транзакцией
- ✅ Полная история всех покупок и продаж
- ✅ Невозможно "потерять" акции или создать их из ниоткуда
- ✅ Правильная бухгалтерия: актив ↔ операция

#### Для анализа:
- ✅ Можно отследить все покупки конкретной акции
- ✅ Можно посчитать среднюю цену покупки по истории
- ✅ Можно увидеть, когда и по какой цене продавали
- ✅ Легко рассчитать реализованную прибыль/убыток

#### Для удобства:
- ✅ Интуитивно понятный процесс: "Купить" → "Продать"
- ✅ Защита от ошибок (валидация количества)
- ✅ Автоматические расчёты (суммы, средние цены)
- ✅ Подтверждение перед важными операциями

### 🎨 UI/UX улучшения

#### Кнопка продажи:
- 🛒 Иконка корзины из магазина (понятный символ продажи)
- Зелёный градиент (положительное действие)
- Увеличение при наведении
- Тень для выделения

#### Модальное окно продажи:
- Читаемые серые поля для информации (неизменяемые данные)
- Автоматическая подстановка текущей цены
- Ограничение максимального количества
- Подсчёт суммы в реальном времени

#### Сообщения:
- Детальная информация о результате операции
- Разные сообщения для полной/частичной продажи
- Подтверждение перед продажей с указанием суммы

### 📊 Пример использования

#### Сценарий: Торговля акциями Сбербанка

**Шаг 1: Покупка**
```
Тикер: SBER
Количество: 10 шт.
Цена: 280 ₽
→ Транзакция: Покупка 10 × 280 ₽ = 2800 ₽
→ Портфель: SBER - 10 шт. по 280 ₽
```

**Шаг 2: Докупка**
```
Тикер: SBER
Количество: 5 шт.
Цена: 290 ₽
→ Транзакция: Покупка 5 × 290 ₽ = 1450 ₽
→ Портфель: SBER - 15 шт. по 283.33 ₽ (средняя)
```

**Шаг 3: Частичная продажа**
```
Продать: 8 шт.
Цена: 295 ₽
→ Транзакция: Продажа 8 × 295 ₽ = 2360 ₽
→ Портфель: SBER - 7 шт. по 283.33 ₽
```

**Шаг 4: Полная продажа**
```
Продать: 7 шт.
Цена: 300 ₽
→ Транзакция: Продажа 7 × 300 ₽ = 2100 ₽
→ Портфель: SBER удалён
```

**История транзакций:**
1. 29.01.2026 14:00 - Покупка SBER - 10 шт. × 280 ₽ = 2800 ₽
2. 29.01.2026 14:30 - Покупка SBER - 5 шт. × 290 ₽ = 1450 ₽
3. 29.01.2026 15:00 - Продажа SBER - 8 шт. × 295 ₽ = 2360 ₽
4. 29.01.2026 15:30 - Продажа SBER - 7 шт. × 300 ₽ = 2100 ₽

**Итого:**
- Куплено: 15 шт. за 4250 ₽ (средняя 283.33 ₽)
- Продано: 15 шт. за 4460 ₽ (средняя 297.33 ₽)
- Прибыль: 210 ₽ (4.94%)

### 🔧 Технические детали

#### Изменённые файлы:
- `static/js/main.js` - логика покупки/продажи
- `templates/index.html` - модальное окно продажи, убрана кнопка
- `static/css/style.css` - стили для кнопки продажи

#### Новые функции JavaScript:
- `openSellModal()` - открытие окна продажи
- `closeSellModal()` - закрытие окна продажи
- `handleSell()` - обработка продажи
- Обновлён `handleAddPosition()` - создание транзакции при покупке
- Обновлён `createPortfolioRow()` - новая кнопка вместо старых

#### API взаимодействие:
- При покупке: `POST /api/transactions` + `POST /api/portfolio`
- При продаже: `POST /api/transactions` + `PUT /api/portfolio/{id}` или `DELETE /api/portfolio/{id}`

### ⚠️ Важные замечания

1. **Редактирование транзакций**
   - В разделе "История покупок/продаж" всё ещё можно редактировать и удалять транзакции
   - Это полезно для исправления ошибок
   - ⚠️ Изменение транзакции НЕ обновляет портфель автоматически

2. **Ручная корректировка портфеля**
   - Если нужно исправить количество в портфеле, используйте DELETE и заново купите правильное количество
   - Или отредактируйте через API напрямую

3. **Средняя цена**
   - При покупке того же тикера средняя цена пересчитывается автоматически
   - Формула: (сумма1 + сумма2) / (количество1 + количество2)

### 🚀 Будущие улучшения

Возможные доработки:
- Синхронизация портфеля с транзакциями (пересчёт портфеля из истории)
- Отчёт о реализованной прибыли
- Калькулятор налогов (FIFO/LIFO)
- Экспорт транзакций для налоговой
- Графики прибыльности отдельных сделок
- История изменения портфеля (timeline)
