# 🔄 Как вернуться к прошлому коммиту в Git

## 📋 Быстрые команды

### 1. Посмотреть историю коммитов
```bash
git log --oneline -10
```

### 2. Временный просмотр коммита (без изменений)
```bash
git checkout <hash_коммита>
# Вернуться обратно:
git checkout main  # или master, или имя вашей ветки
```

### 3. Откат с сохранением изменений (мягкий)
```bash
git reset --soft <hash_коммита>
# Все изменения останутся в staging (готовы к коммиту)
```

### 4. Откат с сохранением изменений в рабочей директории
```bash
git reset --mixed <hash_коммита>
# или просто:
git reset <hash_коммита>
# Изменения останутся в рабочей директории (не в staging)
```

### 5. Полный откат (⚠️ УДАЛЯЕТ ВСЕ ИЗМЕНЕНИЯ)
```bash
git reset --hard <hash_коммита>
# ⚠️ ВНИМАНИЕ: Удаляет все незакоммиченные изменения!
```

### 6. Отмена коммита через новый коммит (безопасно для уже запушенных)
```bash
git revert <hash_коммита>
# Создает новый коммит, отменяющий изменения
```

## 🔍 Примеры

### Вернуться к коммиту "35 attempt" (ecede99):
```bash
# Временный просмотр:
git checkout ecede99

# Откат с сохранением изменений:
git reset --soft ecede99

# Полный откат (удаляет изменения):
git reset --hard ecede99
```

### Вернуться на N коммитов назад:
```bash
# На 1 коммит назад:
git reset --soft HEAD~1

# На 2 коммита назад:
git reset --soft HEAD~2

# На 3 коммита назад:
git reset --soft HEAD~3
```

## ⚠️ Важные предупреждения

1. **Если уже запушили коммиты в удаленный репозиторий:**
   - Используйте `git revert` вместо `git reset`
   - Или используйте `git reset --hard` + `git push --force` (опасно!)

2. **Если есть незакоммиченные изменения:**
   - Сохраните их через `git stash` перед откатом
   - Или закоммитьте их

3. **Если нужно вернуть изменения после `git reset --hard`:**
   - Используйте `git reflog` для поиска потерянных коммитов
   - Затем `git reset --hard <hash_из_reflog>`

## 🔧 Полезные команды

### Сохранить текущие изменения перед откатом:
```bash
git stash
git reset --hard <hash_коммита>
git stash pop  # Вернуть изменения обратно
```

### Посмотреть, что изменится:
```bash
git diff <hash_коммита>
```

### Найти потерянный коммит:
```bash
git reflog
# Затем вернуться к нужному коммиту:
git reset --hard <hash_из_reflog>
```
