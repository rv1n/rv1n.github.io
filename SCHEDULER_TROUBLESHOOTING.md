# 🔧 Диагностика и исправление проблем с планировщиком

## Проблема: Цены не логируются автоматически на сервере

### Шаг 1: Проверка статуса планировщика

Откройте в браузере или через curl:
```
http://ваш-сервер/api/scheduler/status
```

Должен вернуться JSON с информацией:
```json
{
  "success": true,
  "running": true,
  "jobs_count": 2,
  "jobs": [
    {
      "id": "daily_price_logging",
      "name": "Ежедневное логирование цен в 00:00 МСК",
      "next_run_time": "2026-02-19 00:00:00+03:00"
    },
    {
      "id": "periodic_price_logging",
      "name": "Периодическое логирование цен (каждые 3 часа, если записи нет)",
      "next_run_time": "2026-02-18 12:00:00+03:00"
    }
  ]
}
```

**Если `running: false`** - планировщик не запущен. См. Шаг 2.

### Шаг 2: Проверка логов сервера

Проверьте логи Gunicorn или вашего сервиса:

```bash
# Для systemd сервиса
sudo journalctl -u gunicorn -f

# Или для логов Gunicorn
tail -f /var/log/gunicorn/error.log
# или
tail -f /var/log/gunicorn/access.log
```

Ищите строки:
- `===== ПЛАНИРОВЩИК ЗАПУЩЕН =====`
- `ОШИБКА запуска планировщика`
- `НАЧАЛО ЛОГИРОВАНИЯ ЦЕН`

### Шаг 3: Принудительный запуск планировщика

Если планировщик не запускается автоматически, сделайте любой HTTP запрос к приложению (например, откройте главную страницу). Планировщик должен запуститься при первом запросе.

### Шаг 4: Проблема с Gunicorn и несколькими worker'ами

**Важно:** APScheduler не работает корректно с несколькими worker процессами Gunicorn!

#### Решение 1: Запуск с одним worker (для тестирования)

```bash
gunicorn -w 1 app:app
```

#### Решение 2: Использование отдельного процесса для планировщика

Создайте отдельный файл `scheduler_worker.py`:

```python
from app import scheduler, start_scheduler
import time

if __name__ == '__main__':
    start_scheduler()
    print("Планировщик запущен в отдельном процессе")
    try:
        while True:
            time.sleep(60)
    except KeyboardInterrupt:
        scheduler.shutdown()
```

Запустите его отдельно:
```bash
python scheduler_worker.py
```

Или через systemd (создайте `/etc/systemd/system/att1-scheduler.service`):

```ini
[Unit]
Description=Att1 Price Logger Scheduler
After=network.target

[Service]
Type=simple
User=ваш-пользователь
WorkingDirectory=/path/to/att1
Environment="PATH=/path/to/venv/bin"
ExecStart=/path/to/venv/bin/python scheduler_worker.py
Restart=always

[Install]
WantedBy=multi-user.target
```

Затем:
```bash
sudo systemctl enable att1-scheduler
sudo systemctl start att1-scheduler
```

### Шаг 5: Ручное логирование для проверки

Проверьте, что логирование работает вручную:

```bash
curl -X POST http://ваш-сервер/api/log-prices-now
```

Или через интерфейс: нажмите кнопку "Записать цены сейчас" в разделе "История цен".

### Шаг 6: Проверка времени на сервере

Убедитесь, что время на сервере правильное (московское):

```bash
# Проверка времени
date
timedatectl

# Установка часового пояса (если нужно)
sudo timedatectl set-timezone Europe/Moscow
```

### Шаг 7: Проверка настроек времени логирования

Проверьте текущее время логирования:

```bash
curl http://ваш-сервер/api/settings/logging-time
```

Или через интерфейс: меню → "⏰ Настройка времени логирования"

### Шаг 8: Проверка базы данных

Убедитесь, что таблица `settings` существует и содержит настройки:

```bash
sqlite3 portfolio.db "SELECT * FROM settings;"
```

Если таблицы нет, перезапустите приложение - она создастся автоматически.

## Частые проблемы

### Проблема: Планировщик запускается, но задачи не выполняются

**Причина:** Неправильное время или часовой пояс

**Решение:** 
1. Проверьте часовой пояс сервера
2. Установите правильное время логирования через интерфейс
3. Проверьте логи на наличие ошибок

### Проблема: Планировщик запускается в нескольких worker'ах

**Причина:** Gunicorn с несколькими worker процессами

**Решение:** Используйте один worker или отдельный процесс для планировщика (см. Шаг 4)

### Проблема: Ошибки в логах при логировании

**Причина:** Проблемы с MOEX API или базой данных

**Решение:**
1. Проверьте доступность MOEX API
2. Проверьте права доступа к базе данных
3. Проверьте логи на детальные ошибки

## Контакты для диагностики

Если проблема не решена, соберите следующую информацию:

1. Вывод `/api/scheduler/status`
2. Логи сервера (последние 100 строк)
3. Результат `curl -X POST http://ваш-сервер/api/log-prices-now`
4. Время на сервере (`date`)
5. Количество worker процессов Gunicorn
