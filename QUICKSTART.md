# 🚀 Быстрый старт

## Установка и запуск за 3 шага

### 1. Установите зависимости
```bash
pip install -r requirements.txt
```
с
### 2. Запустите приложение
```bash
python app.py
```

### 3. Откройте в браузере
```
http://localhost:5000
```

## Тестирование MOEX API

Для проверки работы с MOEX API:
```bash
python example_usage.py
```

## Добавление первой позиции

1. Введите тикер (например: `SBER`, `GAZP`, `LKOH`)
2. Название компании заполнится автоматически
3. Укажите количество акций
4. Укажите цену покупки
5. Нажмите "Добавить"

## Первое логирование цен (важно!)

После добавления позиций **обязательно выполните первое логирование**:

### Вариант 1: Автоматически
Дождитесь 00:00 по московскому времени - цены залогируются автоматически.

### Вариант 2: Вручную (рекомендуется для начала)
Выполните API запрос:
```bash
curl -X POST http://localhost:5000/api/log-prices-now
```

Или используйте любой HTTP клиент (Postman, Thunder Client).

**Почему это важно?**
- Без записи в истории цен изменение будет показывать 0%
- После логирования вы увидите реальные изменения цен
- Подробнее: [PRICE_CHANGE_LOGIC.md](PRICE_CHANGE_LOGIC.md)

## Популярные тикеры MOEX

- **SBER** - Сбербанк
- **GAZP** - Газпром
- **LKOH** - Лукойл
- **YNDX** - Яндекс
- **GMKN** - Норникель
- **ROSN** - Роснефть
- **NVTK** - Новатэк
- **TATN** - Татнефть

## Структура проекта

```
att1/
├── app.py              # Flask приложение
├── models/             # Модели БД
├── services/           # Сервисы (MOEX API)
├── templates/          # HTML шаблоны
├── static/             # CSS и JavaScript
└── portfolio.db        # База данных (создается автоматически)
```

## Возможные проблемы

### Порт 5000 занят
Измените порт в `app.py`:
```python
app.run(debug=True, host='0.0.0.0', port=8080)
```

### Ошибка подключения к MOEX API
- Проверьте интернет-соединение
- Убедитесь, что тикер существует на MOEX
- Проверьте, что биржа открыта (рабочие часы)

### База данных не создается
Убедитесь, что у вас есть права на запись в директории проекта.

---

**Готово! Наслаждайтесь отслеживанием портфеля! 📈**
